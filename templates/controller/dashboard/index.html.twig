{% extends 'base.html.twig' %}

{% block main %}
    <div class="panel" data-controller="log" data-log-url-value="{{ path('app_dashboard_sysinfo') }}">
        <h3>System</h3>
        <div class="log" data-log-target="console">... fetching</div>
    </div>
    <div class="panel" data-controller="log" data-log-url-value="{{ path('app_dashboard_watchlog') }}">
        <h3>PV</h3>
        <div class="log hardwrap" data-log-target="console">... fetching</div>
    </div>
{% endblock %}
